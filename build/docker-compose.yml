version: "3.0"
services:
  nginx:
    image: app:v1
    ports:
      - "80:80"
    container_name: test1
  jenkins:
    image: jenkins/jenkins:lts
    container_name: jenkins
    ports:
      - "8080:8080"
